import GalleryPage from "@/components/Gallery";

export const metadata = {
  title: "Gallery | B-Estates",
  description:
    "Explore our gallery — land parcels, construction materials, projects, and community work by B-Estates.",
};

export default function Gallery() {
  return <GalleryPage />;
}